import { Time } from '@angular/common';

export class RatesViewModel {
  currency: string;
  rate: string;
  bid: string;
  ask: string;
  high: string;
  low: string;
  open: string;
  close: string;
  timestamp: string;
}
